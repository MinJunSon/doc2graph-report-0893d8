# doc2graph report (published)

doc2graph 버전 변천사 보고서의 발행본입니다. 원본/수정은 메인 repo의 `report/` 에서 하고, `report/publish.ps1` 로 이 repo에 동기화·push 합니다.

갱신: 메인 repo에서 report-data.js 편집 → `python report/build_graph_data.py` → `report/publish.ps1` 실행.
